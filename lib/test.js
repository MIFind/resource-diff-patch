'use strict'
const expect = require('chai').expect
const generatorPatchZip = require('../dist/index').generatorPatchZip
const patchResource = require('../dist/index').patchResource

const PACKAGE_NAME = 'resource-diff-patch'

describe(`${PACKAGE_NAME} function test`, () => {
  it('should return 2', () => {
    
  })
})
